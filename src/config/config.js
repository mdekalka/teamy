export const URL = 'http://jsonplaceholder.typicode.com';

export default {
  url: {
    users: `${URL}/users`,
    photos: `${URL}/photos`,
    albums: `${URL}/albums`,
    comments: `${URL}/comments`,
    posts: `${URL}/posts`
  }
}
